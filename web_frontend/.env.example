# Frontend dev proxy target (Vite)
# Point to your backend in dev. In Docker Compose, set to http://backend:3001
VITE_PROXY_TARGET=http://localhost:3001

# Optional: Override API base used by the app (defaults to /api)
# Set this only if your backend is on a different origin in production.
# VITE_API_BASE=https://api.example.com/api
